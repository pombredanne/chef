---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 30
      retry:
        automatic:
          limit: 1

steps:

#########################################################################
  # Verify - Habitat Plans
#########################################################################

- label: "Windows plan"
  commands:
    - scripts/bk_tests/verify_win_plan.ps1
  expeditor:
    executor:
      windows:
        privileged: true
        single-use: true
        shell: ["powershell", "-Command"]